#include "main.h"
/**
 * mul - returns the product of its parametersfunction that multiplies two integers
 * @a: int type number
 * @b: int type number
 * Return:0
 */

int mul(int a, int b)
{
    return (a * b);
}
